{
  "name": "css-class-builder",
  "version": "1.0.0",
  "description": "A javascript microframework to shortened daily use css class manipulator methods by adding them all into a single method.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/ankitmishradev/css-class-builder.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "format": "prettier --write \"dist/**/*.ts\" \"dist/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "keywords": [
    "css",
    "css-class",
    "css-class-builder",
    "shortened-css-methods"
  ],
  "author": "Ankit Mishra",
  "license": "MIT",
  "files": [
    "dist/**/*"
  ],
  "devDependencies": {
    "prettier": "^2.5.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.5.4"
  }
}
